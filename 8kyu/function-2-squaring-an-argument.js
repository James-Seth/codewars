//Description:
// Now you have to write a function that takes an argument and returns the square of it.

//My Solution:
function square(x) {
    return Math.pow(x, 2)
  }

//Other solutions:
const square = (n) => n * n;

//Another one:
function square(x) {
    return x ** 2;
  }